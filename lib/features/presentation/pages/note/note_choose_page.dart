import 'package:flutter/material.dart';

class NoteChoosePage extends StatelessWidget {
  const NoteChoosePage({
    Key? key,
    required this.userId,
  }) : super(key: key);
  final String userId;

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
